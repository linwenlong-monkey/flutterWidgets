import 'package:flutter/material.dart';

//图标组件，Icon，展示图标，不可以交互，
//可交互的图片，IconButton组件

//IconButton，Icons，IconTheme，ImageIcon

class iconDemo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Icon(
      Icons.wifi,
    );
  }
}
